The algorithm 
```Python
algo(m, inputs[1...])
1 min_heap = [] 
2 for i in inputs
3     if i == PRINT
4         a
5     else
6         a


```
- **Line 1:** declares a min-heap as an empty dynamic array
- The min-heap accumulates **only the integer inputs** until the algorithm reaches a PRINT input